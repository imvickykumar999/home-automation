from setuptools import setup

setup(
    name = 'imvickykumar999', # while installing pacakge, change name to something unique on pypi.org
    version = '0.0.8', # use different version if updated, like '0.0.2'
    description = 'https://github.com/imvickykumar999/home-automation',
    long_description = open('README.md').read(),
    url = 'https://imvickykumar999.herokuapp.com/',
    author = 'Vicky Kumar',
    keywords = ['Firebase', 'ESP32', 'Home Automation', 'PyPi Uploader', 'GitHub Uploader','Morse','custom','python package','function and class','3D line',
    '3D plane', 'angle bw planes or line', 'distance bw point and plane'],
    license = 'MIT',
    packages = ['multivicks', 'vicksbase'], # while importing package
    install_requires = ['']
)
